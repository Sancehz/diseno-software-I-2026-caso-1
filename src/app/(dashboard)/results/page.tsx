/**
 * Results page — receives the generated DUA document via query params or
 * process store; delegates rendering to ResultsPage component.
 */
export default function Page() {
  // TODO: hydrate DuaDocument from process store or server-side fetch
  return <div>Results</div>;
}
